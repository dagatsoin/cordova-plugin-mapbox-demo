import { Styles } from '@sproutch/ui';

export const button = Styles.createViewStyle({
  margin: 8,
  flex: 1
})

export const actionContainer = Styles.createViewStyle({
  flexDirection:"row",
  flexWrap:"wrap"
})

export const progressBar = Styles.createViewStyle({
  height: 24
})